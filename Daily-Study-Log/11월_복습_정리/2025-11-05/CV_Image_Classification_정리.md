## 🧩 Image Classification 정리

---

### 📘 **개념**

* **Image Classification(이미지 분류)** 은
  컴퓨터 비전 분야에서 **가장 대표적이고 성공적인 Data-driven 태스크 중 하나**

* 즉, 모델이 **이미지 내의 객체를 인식하고 해당 이미지가 어떤 클래스(라벨)에 속하는지** 판단하는 문제

---

### ⚙️ **Data-driven 접근 방식**

* 과거에는 사람이 직접 특징(feature)을 정의했지만,
  **딥러닝 이후 모델이 스스로 특징을 추출하고 학습**

* 즉, 데이터를 충분히 주면 모델이

  * 어떤 특징이 중요한지 판단하고
  * 그 특징을 바탕으로 학습하며
  * 마지막에는 추론까지 **전 과정을 스스로 수행**

✅ **핵심 포인트**

> 사람이 개입하지 않고 **데이터만으로 학습이 가능한 구조**

---

### 🔄 **전체 흐름**

#### 1️⃣ **데이터 로드 (Data Loading)**

* 이미지 데이터셋을 불러오는 단계
* 학습(train) / 검증(val) / 테스트(test) 데이터로 구분
* `PyTorch Dataset`, `DataLoader` 등을 활용

#### 2️⃣ **데이터 전처리 및 증강 (Preprocessing & Augmentation)**

* 이미지 크기 조정, 정규화, 노이즈 제거
* 학습 데이터 다양성을 높이기 위해 **증강(Augmentation)** 적용
  (예: 회전, 좌우반전, 밝기 조절 등)

#### 3️⃣ **모델 학습 (Model Training)**

* CNN(Convolutional Neural Network) 기반 모델 사용
* 손실 함수(Loss Function) 최소화 방향으로 가중치 업데이트
* 옵티마이저(Optimizer)로 학습 진행 (`Adam`, `SGD` 등)

#### 4️⃣ **추론 (Inference)**

* 학습된 모델을 이용해 **새로운 이미지의 클래스 예측**
* 예: “이 이미지는 ‘고양이’일 확률 92%”

---

### 💡 **요약**

| 단계     | 내용           | 주요 키워드                     |
| ------ | ------------ | -------------------------- |
| 데이터 로드 | 데이터셋 불러오기    | Dataset, DataLoader        |
| 전처리/증강 | 이미지 정규화 및 변형 | Resize, Normalize, Flip    |
| 모델 학습  | CNN 모델로 학습   | Conv Layer, ReLU, Pooling  |
| 추론     | 학습된 모델로 예측   | Softmax, Class Probability |

---

📊 **정리 문장**

> Image Classification은 방대한 데이터를 기반으로
> 모델이 직접 특징을 추출하고 학습하여,
> 사람이 개입하지 않아도 스스로 이미지를 분류할 수 있는
> 대표적인 **Data-driven Computer Vision Task**이다.

---


